[package]
name = "aca"
version = "0.1.0"
edition = "2021"

[features]

[dependencies]
lru-mem = "0.2.0"
strum = { version = "0.24", features = ["derive"] }

[dev-dependencies]
generic-tests = "0.1.2"
